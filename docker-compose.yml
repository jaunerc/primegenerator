version: '3'
services:
  backend:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
  frontend:
    build:
      context: ./react-frontend
      dockerfile: ./Dockerfile
    ports:
      - "80:80"